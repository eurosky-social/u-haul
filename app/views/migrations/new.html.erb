<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="noindex, nofollow, noarchive">
  <meta name="googlebot" content="noindex, nofollow, noarchive">
  <%= csrf_meta_tags %>
  <title><%= EuroskyConfig::SITE_NAME %> - Start Account Migration</title>
  <% if EuroskyConfig::FAVICON_URL.present? %>
    <link rel="icon" type="image/png" href="<%= EuroskyConfig::FAVICON_URL %>">
  <% end %>
  <%= render 'shared/styles' %>
  <style>
    body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      <% if EuroskyConfig::BACKGROUND_IMAGE_URL.present? %>
      background: url('<%= EuroskyConfig::BACKGROUND_IMAGE_URL %>') no-repeat center center fixed;
      background-size: cover;
      <% else %>
      background: var(--gradient);
      <% end %>
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }

    <% if EuroskyConfig::BACKGROUND_IMAGE_URL.present? %>
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.4);
      z-index: 0;
    }
    <% end %>

    .container {
      position: relative;
      z-index: 1;
      max-width: 700px;
      width: 90%;
      margin: 40px auto;
    }

    .content {
      background: rgba(255, 255, 255, 0.92);
      backdrop-filter: blur(20px) saturate(180%);
      border-radius: 24px;
      padding: 48px;
      padding-top: 0;
      border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .header {
      text-align: center;
      margin-bottom: 24px;
      padding: 24px 48px;
      background: #ffffff;
      border-radius: 12px;
    }

    .header h1 {
      font-size: 72px;
      font-weight: 900;
      color: #000000;
      margin: 0 0 8px 0;
      letter-spacing: -2px;
      font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", "Arial Black", sans-serif;
    }

    .header p {
      font-size: 14px;
      color: #000000;
      margin: 0;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .wizard-step {
      display: none;
    }

    .wizard-step.active {
      display: block;
    }

    .wizard-progress {
      display: flex;
      justify-content: space-between;
      margin-bottom: 32px;
      padding: 0 20px;
    }

    .wizard-progress-step {
      flex: 1;
      text-align: center;
      position: relative;
    }

    .wizard-progress-step::before {
      content: '';
      position: absolute;
      top: 15px;
      left: -50%;
      right: 50%;
      height: 2px;
      background-color: #ddd;
      z-index: -1;
    }

    .wizard-progress-step:first-child::before {
      display: none;
    }

    .wizard-progress-step.active::before,
    .wizard-progress-step.completed::before {
      background-color: #F57F29;
    }

    .wizard-progress-circle {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      background: #e0e0e0;
      color: white;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      margin-bottom: 8px;
      transition: all 0.3s ease;
    }

    .wizard-progress-step.active .wizard-progress-circle {
      background: #F57F29;
      transform: scale(1.1);
    }

    .wizard-progress-step.completed .wizard-progress-circle {
      background: #28a745;
    }

    .wizard-progress-label {
      font-size: 13px;
      color: #666;
      font-weight: 500;
    }

    .wizard-progress-step.active .wizard-progress-label {
      color: #F57F29;
      font-weight: 600;
    }

    .wizard-navigation {
      display: flex;
      gap: 12px;
      margin-top: 24px;
    }

    .wizard-back-btn {
      background-color: #6c757d;
      color: white;
      border: none;
      padding: 12px 24px;
      font-size: 16px;
      border-radius: 4px;
      cursor: pointer;
      font-weight: 500;
    }

    .wizard-back-btn:hover {
      background-color: #5a6268;
    }

    .readonly-field {
      background-color: #f5f5f5;
      color: #666;
      cursor: not-allowed;
    }

    .lookup-status {
      margin-top: 12px;
      padding: 8px 12px;
      border-radius: 4px;
      font-size: 14px;
      display: none;
    }

    .lookup-info {
      background-color: #e3f2fd;
      color: #1565c0;
      border: 1px solid #90caf9;
    }

    .lookup-success {
      background-color: #e8f5e9;
      color: #2e7d32;
      border: 1px solid #81c784;
    }

    .lookup-error {
      background-color: #ffebee;
      color: #c62828;
      border: 1px solid #ef9a9a;
    }

    .form-select {
      width: 100%;
      padding: 16px;
      font-size: 18px;
      border: 2px solid #ddd;
      border-radius: 4px;
      background-color: white;
      cursor: pointer;
      transition: border-color 0.2s;
    }

    .form-select:focus {
      outline: none;
      border-color: #007bff;
    }

    .form-select:hover {
      border-color: #bbb;
    }

    .form-control {
      width: 100%;
      padding: 12px;
      font-size: 16px;
      border: 2px solid #ddd;
      border-radius: 4px;
      transition: border-color 0.2s;
    }

    .form-control:focus {
      outline: none;
      border-color: #007bff;
    }

    .warning-box {
      margin-top: 32px;
    }
  </style>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.querySelector('form');

      // Form fields
      const handleField = document.getElementById('migration_old_handle');
      const passwordField = document.getElementById('migration_password');
      const emailField = document.getElementById('migration_email');
      const pdsSelect = document.getElementById('pds-select');
      const customPdsField = document.getElementById('migration_new_pds_host');
      const inviteCodeField = document.getElementById('migration_invite_code');

      // Steps
      const step1 = document.getElementById('step-1');
      const step2 = document.getElementById('step-2');
      const step3 = document.getElementById('step-3');
      const step4 = document.getElementById('step-4');
      const step5 = document.getElementById('step-5');

      // Wizard state
      let wizardData = {
        email: null,
        emailReadonly: false,
        authenticated: false,
        pdsHost: null,
        pdsVerified: false,         // true after checkPdsRequirements succeeds
        pdsVerifiedHost: null,      // the host that was verified
        availableDomains: [],
        handleType: null,           // 'dns_verified' or 'pds_hosted'
        canPreserveHandle: false,   // true if custom domain handle can be preserved
        oldHandle: null,            // original handle for preservation
        handleVerifiedVia: null     // 'dns', 'http_wellknown', or 'pds_api'
      };

      // Progress indicators
      const progress1 = document.getElementById('progress-1');
      const progress2 = document.getElementById('progress-2');
      const progress3 = document.getElementById('progress-3');
      const progress4 = document.getElementById('progress-4');
      const progress5 = document.getElementById('progress-5');

      // Configuration from server
      const defaultTargetPds = '<%= EuroskyConfig::DEFAULT_TARGET_PDS %>';

      // Initialize
      showStep(1);

      // Pre-select default target PDS if configured
      if (defaultTargetPds && pdsSelect) {
        // Check if the default PDS is in the select options
        const option = Array.from(pdsSelect.options).find(opt => opt.value === defaultTargetPds);
        if (option) {
          pdsSelect.value = defaultTargetPds;
          customPdsField.value = defaultTargetPds;
        }
      }

      // Show/hide steps
      function showStep(stepNumber) {
        [step1, step2, step3, step4, step5].forEach(s => s.classList.remove('active'));
        [progress1, progress2, progress3, progress4, progress5].forEach(p => p.classList.remove('active', 'completed'));

        if (stepNumber === 1) {
          step1.classList.add('active');
          progress1.classList.add('active');
        }
        if (stepNumber === 2) {
          step2.classList.add('active');
          progress1.classList.add('completed');
          progress2.classList.add('active');
        }
        if (stepNumber === 3) {
          step3.classList.add('active');
          progress1.classList.add('completed');
          progress2.classList.add('completed');
          progress3.classList.add('active');
        }
        if (stepNumber === 4) {
          step4.classList.add('active');
          progress1.classList.add('completed');
          progress2.classList.add('completed');
          progress3.classList.add('completed');
          progress4.classList.add('active');
        }
        if (stepNumber === 5) {
          step5.classList.add('active');
          [progress1, progress2, progress3, progress4].forEach(p => p.classList.add('completed'));
          progress5.classList.add('active');
        }
      }

      // Utility: Show status message
      function showStatus(element, message, type) {
        element.textContent = message;
        element.className = 'lookup-status lookup-' + type;
        element.style.display = 'block';
      }

      // STEP 1: Handle & Password
      document.getElementById('step1-next').addEventListener('click', function(e) {
        e.preventDefault();

        const handle = handleField.value.trim();
        const password = passwordField.value.trim();
        const btn = e.target;
        const status = document.getElementById('step1-status');

        if (!handle) {
          showStatus(status, 'Please enter a handle', 'error');
          return;
        }

        if (!password) {
          showStatus(status, 'Please enter your password', 'error');
          return;
        }

        if (!handle.includes('.')) {
          showStatus(status, 'Handle must include a domain (e.g., user.bsky.social)', 'error');
          return;
        }

        // If already authenticated and fields are readonly, proceed to step 2
        if (wizardData.authenticated) {
          showStep(2);
          setupStep2();
          return;
        }

        btn.disabled = true;
        btn.textContent = 'Authenticating...';
        showStatus(status, 'Authenticating and fetching your account details...', 'info');

        fetch('/migrations/lookup_handle', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
          },
          body: JSON.stringify({ handle: handle, password: password })
        })
        .then(response => response.json())
        .then(data => {
          if (data.error) {
            showStatus(status, data.error, 'error');
            btn.disabled = false;
            btn.textContent = 'Continue';
            return;
          }

          showStatus(status, 'Authentication successful!', 'success');

          // Store email data and mark as authenticated
          wizardData.authenticated = true;
          if (data.email) {
            wizardData.email = data.email;
            wizardData.emailReadonly = true;
          } else {
            wizardData.email = '';
            wizardData.emailReadonly = false;
          }

          // Store DID, source PDS, and authentication tokens (will be submitted with form)
          wizardData.did = data.did;
          wizardData.oldPdsHost = data.pds_host;
          wizardData.accessToken = data.access_token;
          wizardData.refreshToken = data.refresh_token;

          // Store handle type information for step 4
          wizardData.handleType = data.handle_type || 'pds_hosted';
          wizardData.canPreserveHandle = data.can_preserve_handle || false;
          wizardData.oldHandle = handle;
          wizardData.handleVerifiedVia = data.handle_verified_via;

          // Proceed to step 2
          setTimeout(() => {
            handleField.readOnly = true;
            passwordField.readOnly = true;
            btn.disabled = false;
            btn.textContent = 'Continue';
            showStep(2);
            setupStep2();
          }, 500);
        })
        .catch(error => {
          showStatus(status, 'Network error. Please try again.', 'error');
          btn.disabled = false;
          btn.textContent = 'Continue';
        });
      });

      // STEP 2: Email
      function setupStep2() {
        emailField.value = wizardData.email || '';
        if (wizardData.emailReadonly) {
          emailField.readOnly = true;
          emailField.classList.add('readonly-field');
          document.querySelector('#step-2 .helper-text').textContent = 'Email retrieved from your account';
        } else {
          emailField.readOnly = false;
          emailField.classList.remove('readonly-field');
          document.querySelector('#step-2 .helper-text').textContent = 'We couldn\'t retrieve your email. Please enter it manually.';
          emailField.focus();
        }
      }

      document.getElementById('step2-back').addEventListener('click', function(e) {
        e.preventDefault();
        // Unlock fields so user can edit
        handleField.readOnly = false;
        passwordField.readOnly = false;
        handleField.classList.remove('readonly-field');
        passwordField.classList.remove('readonly-field');
        // Reset authentication state
        wizardData.authenticated = false;
        showStep(1);
      });

      document.getElementById('step2-next').addEventListener('click', function(e) {
        e.preventDefault();

        const email = emailField.value.trim();
        if (!email) {
          alert('Please enter your email address');
          return;
        }

        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!emailRegex.test(email)) {
          alert('Please enter a valid email address');
          return;
        }

        const confirmCheckbox = document.getElementById('confirm-email-access');
        if (!confirmCheckbox.checked) {
          alert('Please confirm that you have access to this email address.');
          confirmCheckbox.focus();
          return;
        }

        wizardData.email = email;
        emailField.readOnly = true;
        showStep(3);
        setupStep3();
      });

      // STEP 3: PDS Selection
      // Setup Step 3: Check PDS requirements if default is set
      function setupStep3() {
        // If default PDS is configured and selected, check requirements automatically
        if (defaultTargetPds && pdsSelect.value === defaultTargetPds) {
          const status = document.getElementById('pds-status');
          const inviteGroup = document.getElementById('invite-code-group');
          checkPdsRequirements(defaultTargetPds, status, inviteGroup);
          updateTargetPasswordVisibility(defaultTargetPds);
        }
      }

      document.getElementById('step3-back').addEventListener('click', function(e) {
        e.preventDefault();
        if (!wizardData.emailReadonly) {
          emailField.readOnly = false;
        }
        showStep(2);
      });

      document.getElementById('step3-next').addEventListener('click', function(e) {
        e.preventDefault();

        const btn = e.target;
        const status = document.getElementById('pds-status');
        const inviteGroup = document.getElementById('invite-code-group');
        let selectedPds = pdsSelect.value;

        // If "custom" is selected, use the custom PDS field value
        if (selectedPds === 'custom') {
          selectedPds = customPdsField.value.trim();
        }

        if (!selectedPds || selectedPds === 'custom') {
          alert('Please select a PDS or enter a custom PDS URL');
          return;
        }

        // For custom PDS: if not yet verified, check requirements first
        if (pdsSelect.value === 'custom' && (!wizardData.pdsVerified || wizardData.pdsVerifiedHost !== selectedPds)) {
          btn.disabled = true;
          btn.textContent = 'Checking PDS...';
          checkPdsRequirements(selectedPds, status, inviteGroup, function(success) {
            btn.disabled = false;
            btn.textContent = 'Continue';
            if (success) {
              updateTargetPasswordVisibility(selectedPds);
              // If invite code is now required, stay on this step so user can fill it in
              if (inviteCodeField.required) {
                showStatus(status, 'This PDS requires an invite code. Please enter it below, then click Continue.', 'info');
                inviteCodeField.focus();
              }
            }
          });
          return;
        }

        // Check if invite code is required and empty
        if (inviteCodeField.required && !inviteCodeField.value.trim()) {
          showStatus(status, 'Please enter an invite code to continue.', 'error');
          inviteCodeField.focus();
          return;
        }

        // For migration_in (returning to bsky.social), verify target PDS credentials
        if (isMigrationIn(selectedPds)) {
          const targetPassword = document.getElementById('target-pds-password').value.trim();
          const targetStatus = document.getElementById('target-password-status');

          if (!targetPassword) {
            showStatus(targetStatus, 'Please enter your bsky.social password', 'error');
            return;
          }

          // If already verified, proceed
          if (wizardData.targetCredentialsVerified) {
            wizardData.pdsHost = selectedPds;
            showStep(4);
            setupStep4();
            return;
          }

          btn.disabled = true;
          btn.textContent = 'Verifying bsky.social credentials...';
          showStatus(targetStatus, 'Authenticating with your bsky.social account...', 'info');

          fetch('/migrations/verify_target_credentials', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
              'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
            },
            body: JSON.stringify({
              pds_host: selectedPds,
              did: wizardData.did,
              password: targetPassword
            })
          })
          .then(response => response.json())
          .then(data => {
            btn.disabled = false;
            btn.textContent = 'Continue';

            if (data.error) {
              showStatus(targetStatus, data.error, 'error');
              return;
            }

            // Store target PDS tokens
            wizardData.targetCredentialsVerified = true;
            wizardData.newPdsAccessToken = data.access_token;
            wizardData.newPdsRefreshToken = data.refresh_token;

            showStatus(targetStatus, 'bsky.social credentials verified!', 'success');

            // Proceed to step 4
            setTimeout(() => {
              wizardData.pdsHost = selectedPds;
              showStep(4);
              setupStep4();
            }, 500);
          })
          .catch(error => {
            btn.disabled = false;
            btn.textContent = 'Continue';
            showStatus(targetStatus, 'Network error. Please try again.', 'error');
          });
          return;
        }

        // All checks passed, store PDS and proceed to step 4
        wizardData.pdsHost = selectedPds;
        showStep(4);
        setupStep4();
      });

      // Helper: Check if selecting this PDS makes it a migration_in scenario
      function isMigrationIn(pdsUrl) {
        if (!pdsUrl || !wizardData.oldPdsHost) return false;
        const targetHost = pdsUrl.toLowerCase().replace(/^https?:\/\//, '');
        const sourceHost = wizardData.oldPdsHost.toLowerCase().replace(/^https?:\/\//, '');
        return targetHost.includes('bsky.social') && !sourceHost.includes('bsky.social');
      }

      // Helper: Show/hide the target password group based on migration_in detection
      function updateTargetPasswordVisibility(pdsUrl) {
        const targetPasswordGroup = document.getElementById('target-password-group');
        const targetPasswordStatus = document.getElementById('target-password-status');
        if (isMigrationIn(pdsUrl)) {
          targetPasswordGroup.style.display = 'block';
          // Reset verification state
          wizardData.targetCredentialsVerified = false;
          wizardData.newPdsAccessToken = null;
          wizardData.newPdsRefreshToken = null;
          targetPasswordStatus.style.display = 'none';
        } else {
          targetPasswordGroup.style.display = 'none';
          wizardData.targetCredentialsVerified = false;
          wizardData.newPdsAccessToken = null;
          wizardData.newPdsRefreshToken = null;
        }
      }

      // PDS selection
      pdsSelect.addEventListener('change', function(e) {
        const selectedValue = e.target.value;
        const customGroup = document.getElementById('custom-pds-group');
        const inviteGroup = document.getElementById('invite-code-group');
        const status = document.getElementById('pds-status');

        // Reset PDS verification when selection changes
        wizardData.pdsVerified = false;
        wizardData.pdsVerifiedHost = null;

        if (selectedValue === 'custom') {
          customGroup.style.display = 'block';
          customPdsField.value = '';
          customPdsField.focus();
          inviteGroup.style.display = 'none';
          status.style.display = 'none';
          updateTargetPasswordVisibility(null);
        } else if (selectedValue) {
          customGroup.style.display = 'none';
          customPdsField.value = selectedValue;
          checkPdsRequirements(selectedValue, status, inviteGroup);
          updateTargetPasswordVisibility(selectedValue);
        } else {
          customGroup.style.display = 'none';
          inviteGroup.style.display = 'none';
          status.style.display = 'none';
          updateTargetPasswordVisibility(null);
        }
      });

      // Reset verification when user edits the custom PDS field
      customPdsField.addEventListener('input', function() {
        if (pdsSelect.value === 'custom') {
          wizardData.pdsVerified = false;
          wizardData.pdsVerifiedHost = null;
        }
      });

      // Check PDS requirements
      // Optional callback(success) is called when the check completes
      function checkPdsRequirements(pdsHost, statusElement, inviteGroup, callback) {
        showStatus(statusElement, 'Checking PDS requirements...', 'info');

        fetch('/migrations/check_pds', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
          },
          body: JSON.stringify({ pds_host: pdsHost })
        })
        .then(response => response.json())
        .then(data => {
          if (data.error) {
            showStatus(statusElement, data.error, 'error');
            inviteGroup.style.display = 'none';
            wizardData.pdsVerified = false;
            if (callback) callback(false);
            return;
          }

          showStatus(statusElement, 'PDS verified!', 'success');

          // Store available domains for step 4
          wizardData.availableDomains = data.available_user_domains || [];
          wizardData.pdsVerified = true;
          wizardData.pdsVerifiedHost = pdsHost;

          if (data.invite_code_required) {
            inviteGroup.style.display = 'block';
            inviteCodeField.required = true;
            inviteCodeField.setAttribute('required', 'required');
          } else {
            inviteGroup.style.display = 'none';
            inviteCodeField.required = false;
            inviteCodeField.removeAttribute('required');
          }
          if (callback) callback(true);
        })
        .catch(error => {
          showStatus(statusElement, 'Failed to verify PDS. Please check the URL.', 'error');
          inviteGroup.style.display = 'none';
          wizardData.pdsVerified = false;
          if (callback) callback(false);
        });
      }

      // STEP 4: New Handle
      const newHandleInput = document.getElementById('new-handle-input');
      const newHandleSuffix = document.getElementById('new-handle-suffix');
      const preservationSection = document.getElementById('handle-preservation-section');
      const newHandleSection = document.getElementById('new-handle-section');
      const preserveCheckbox = document.getElementById('preserve-handle-checkbox');
      const customHandleDisplay = document.getElementById('custom-handle-display');

      function setupStep4() {
        // Check if user has a custom domain handle
        if (wizardData.canPreserveHandle && wizardData.handleType === 'dns_verified') {
          // Show preservation option
          preservationSection.style.display = 'block';
          customHandleDisplay.textContent = '@' + wizardData.oldHandle;
          preserveCheckbox.checked = true;

          // Hide new handle input initially (show only if they uncheck)
          newHandleSection.style.display = 'none';
        } else {
          // PDS-hosted handle - must choose new handle
          preservationSection.style.display = 'none';
          newHandleSection.style.display = 'block';

          // Set the domain suffix
          if (wizardData.availableDomains.length > 0) {
            const domain = wizardData.availableDomains[0];
            newHandleSuffix.textContent = domain.startsWith('.') ? domain : '.' + domain;
          } else {
            // Extract domain from PDS host
            const pdsUrl = new URL(wizardData.pdsHost);
            newHandleSuffix.textContent = '.' + pdsUrl.hostname;
          }
          newHandleInput.value = '';
          newHandleInput.focus();
        }
      }

      // Toggle new handle input when preservation checkbox changes
      preserveCheckbox.addEventListener('change', function(e) {
        if (e.target.checked) {
          // Preserving custom handle
          newHandleSection.style.display = 'none';
        } else {
          // Creating new handle
          newHandleSection.style.display = 'block';

          // Set the domain suffix
          if (wizardData.availableDomains.length > 0) {
            const domain = wizardData.availableDomains[0];
            newHandleSuffix.textContent = domain.startsWith('.') ? domain : '.' + domain;
          } else {
            const pdsUrl = new URL(wizardData.pdsHost);
            newHandleSuffix.textContent = '.' + pdsUrl.hostname;
          }
          newHandleInput.value = '';
          newHandleInput.focus();
        }
      });

      document.getElementById('step4-back').addEventListener('click', function(e) {
        e.preventDefault();
        showStep(3);
      });

      document.getElementById('step4-next').addEventListener('click', function(e) {
        e.preventDefault();

        const btn = e.target;
        const status = document.getElementById('step4-status');
        let fullHandle;

        // Check if user is preserving their custom domain handle
        if (wizardData.canPreserveHandle && preserveCheckbox.checked) {
          // Use the original handle
          fullHandle = wizardData.oldHandle;

          // Store in hidden field and proceed immediately (no availability check needed)
          document.getElementById('migration_new_handle').value = fullHandle;
          showStatus(status, 'Keeping your custom domain handle!', 'success');

          setTimeout(() => {
            showStep(5);
            setupStep5();
          }, 500);
          return;
        }

        // User is creating a new handle - validate and check availability
        const handleUsername = newHandleInput.value.trim();
        if (!handleUsername) {
          alert('Please enter a handle');
          return;
        }

        // Construct handle properly, avoiding double dots
        // Cases:
        // 1. "user" + ".pds.example.com" → "user.pds.example.com"
        // 2. "user." + ".pds.example.com" → "user.pds.example.com" (remove duplicate dot)
        // 3. "user" + "pds.example.com" → "user.pds.example.com"
        // 4. "user." + "pds.example.com" → "user.pds.example.com"
        const suffix = newHandleSuffix.textContent;
        const userEndsWithDot = handleUsername.endsWith('.');
        const suffixStartsWithDot = suffix.startsWith('.');

        if (userEndsWithDot && suffixStartsWithDot) {
          // Remove trailing dot from username to avoid double dots
          fullHandle = handleUsername.slice(0, -1) + suffix;
        } else if (!userEndsWithDot && !suffixStartsWithDot) {
          // Add a dot between them
          fullHandle = handleUsername + '.' + suffix;
        } else {
          // One of them has a dot, concatenate directly
          fullHandle = handleUsername + suffix;
        }

        btn.disabled = true;
        btn.textContent = 'Checking availability...';
        showStatus(status, 'Checking if handle is available...', 'info');

        // Check handle availability (pass DID so the user can reclaim their own handle)
        fetch('/migrations/check_handle', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
          },
          body: JSON.stringify({
            handle: fullHandle,
            pds_host: wizardData.pdsHost,
            did: wizardData.did
          })
        })
        .then(response => response.json())
        .then(data => {
          btn.disabled = false;
          btn.textContent = 'Continue';

          if (data.error) {
            showStatus(status, data.error, 'error');
            return;
          }

          if (!data.available) {
            showStatus(status, 'This handle is already taken. Please choose another.', 'error');
            return;
          }

          // Handle is available, now check if DID already has an account on the target PDS
          showStatus(status, 'Handle available, checking for orphaned accounts...', 'info');
          btn.disabled = true;
          btn.textContent = 'Checking for orphaned accounts...';

          fetch('/migrations/check_did_on_pds', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
              'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
            },
            body: JSON.stringify({
              did: wizardData.did,
              pds_host: wizardData.pdsHost,
              old_pds_host: wizardData.oldPdsHost
            })
          })
          .then(response => response.json())
          .then(didData => {
            btn.disabled = false;
            btn.textContent = 'Continue';

            if (didData.error) {
              showStatus(status, didData.error, 'error');
              return;
            }

            if (didData.exists) {
              // Build detailed error message with support contact information
              const supportEmail = didData.support_email || 'support@example.com';
              const errorMessage = `
                <div style="text-align: left; line-height: 1.6;">
                  <strong>👥 Account Already Exists on Target PDS</strong><br><br>
                  <strong>What happened:</strong><br>
                  An account with your DID already exists on the target PDS (${wizardData.pdsHost}).
                  This orphaned account is likely from a previous failed migration attempt.<br><br>

                  <strong>What to do:</strong><br>
                  📧 Contact the target PDS provider to remove the orphaned account:<br>
                  <div style="margin-left: 20px;">
                    • Email: <strong>${supportEmail}</strong><br>
                    • Include your DID: <code style="background: #f0f0f0; padding: 2px 4px;">${wizardData.did}</code><br>
                    • Include your old handle: <strong>${wizardData.oldHandle || wizardData.identifier}</strong><br>
                    ${didData.deactivated ? '• Mention the account is <strong>deactivated/orphaned</strong><br>' : ''}
                  </div><br>

                  ⚠️ <strong>This requires action from the PDS provider - you cannot fix this yourself.</strong><br>
                  Once the orphaned account is removed, you can retry this migration.
                </div>
              `;

              status.className = 'status-message error';
              status.innerHTML = errorMessage;
              status.style.display = 'block';
              return;
            }

            // All checks passed - handle is available and no orphaned account
            showStatus(status, 'All checks passed!', 'success');

            // Store in hidden field and proceed
            document.getElementById('migration_new_handle').value = fullHandle;

            setTimeout(() => {
              showStep(5);
              setupStep5();
            }, 500);
          })
          .catch(didError => {
            btn.disabled = false;
            btn.textContent = 'Continue';
            showStatus(status, 'Failed to check for orphaned accounts: ' + didError.message, 'error');
          });
        })
        .catch(error => {
          btn.disabled = false;
          btn.textContent = 'Continue';
          showStatus(status, 'Failed to check availability. Please try again.', 'error');
        });
      });

      // STEP 5: Summary
      function setupStep5() {
        document.getElementById('summary-handle').textContent = handleField.value;
        document.getElementById('summary-email').textContent = emailField.value;
        document.getElementById('summary-pds').textContent = wizardData.pdsHost;
        document.getElementById('summary-new-handle').textContent = document.getElementById('migration_new_handle').value;

        if (inviteCodeField.value.trim()) {
          document.getElementById('summary-invite-row').style.display = 'table-row';
          document.getElementById('summary-invite').textContent = inviteCodeField.value;
        } else {
          document.getElementById('summary-invite-row').style.display = 'none';
        }
      }

      document.getElementById('step5-back').addEventListener('click', function(e) {
        e.preventDefault();
        showStep(4);
      });

      // Handle form submission
      form.addEventListener('submit', function(event) {
        event.preventDefault();

        // Only allow form submission if we're on the final step (step 5)
        const currentStep = document.querySelector('.wizard-step.active');
        if (!currentStep || currentStep.id !== 'step-5') {
          // User pressed Enter on an intermediate step - do nothing
          return;
        }

        // Check if user has acknowledged the no-usage warning
        const acknowledgeCheckbox = document.getElementById('acknowledge-no-usage');
        if (!acknowledgeCheckbox.checked) {
          alert('Please confirm that you understand you must not use your account during migration.');
          acknowledgeCheckbox.focus();
          return;
        }

        const confirmed = confirm('Are you ready to start the migration?\n\nREMEMBER: Do NOT use your account until you receive the completion email (approximately 20-30 minutes).\n\nThis process will begin immediately and cannot be stopped once started.');
        if (confirmed) {
          // Populate hidden token fields with stored values
          document.getElementById('migration_old_access_token').value = wizardData.accessToken || '';
          document.getElementById('migration_old_refresh_token').value = wizardData.refreshToken || '';

          // For migration_in, also populate target PDS tokens
          document.getElementById('migration_new_access_token').value = wizardData.newPdsAccessToken || '';
          document.getElementById('migration_new_refresh_token').value = wizardData.newPdsRefreshToken || '';

          // Clear password fields before submission (we're using tokens instead)
          passwordField.value = '';
          const targetPwdField = document.getElementById('target-pds-password');
          if (targetPwdField) targetPwdField.value = '';

          form.submit();
        }
      });
    });
  </script>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><%= EuroskyConfig::SITE_NAME %></h1>
      <p><%= EuroskyConfig::SITE_SUBTITLE %></p>
    </div>

    <div class="content">
      <% if @migration&.errors&.any? %>
        <div class="error-messages" role="alert">
          <h3>Please correct the following errors:</h3>
          <ul>
            <% @migration.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with model: @migration, url: migrations_path, local: true, html: { novalidate: true } do |f| %>
        <!-- Hidden fields for authentication tokens (populated by JavaScript after AJAX auth) -->
        <%= hidden_field_tag 'migration[old_access_token]', '', id: 'migration_old_access_token' %>
        <%= hidden_field_tag 'migration[old_refresh_token]', '', id: 'migration_old_refresh_token' %>

        <!-- Progress Indicator -->
        <div class="wizard-progress">
          <div id="progress-1" class="wizard-progress-step">
            <div class="wizard-progress-circle">1</div>
            <div class="wizard-progress-label">Account</div>
          </div>
          <div id="progress-2" class="wizard-progress-step">
            <div class="wizard-progress-circle">2</div>
            <div class="wizard-progress-label">Email</div>
          </div>
          <div id="progress-3" class="wizard-progress-step">
            <div class="wizard-progress-circle">3</div>
            <div class="wizard-progress-label">Destination</div>
          </div>
          <div id="progress-4" class="wizard-progress-step">
            <div class="wizard-progress-circle">4</div>
            <div class="wizard-progress-label">Handle</div>
          </div>
          <div id="progress-5" class="wizard-progress-step">
            <div class="wizard-progress-circle">5</div>
            <div class="wizard-progress-label">Review</div>
          </div>
        </div>

        <!-- STEP 1: Handle & Password -->
        <div id="step-1" class="wizard-step">
          <h2>Step 1: Account Credentials</h2>

          <div class="form-group">
            <%= f.label :old_handle, "Your Handle" %>
            <%= f.text_field :old_handle, id: "migration_old_handle", placeholder: "yourhandle.bsky.social", autocomplete: "username" %>
            <div class="helper-text">Enter your current AT Protocol handle</div>
          </div>

          <div class="form-group">
            <%= f.label :password, "Account Password" %>
            <%= f.password_field :password, id: "migration_password", autocomplete: "current-password" %>
            <div class="helper-text">Your current account password</div>
          </div>

          <div id="step1-status" class="lookup-status"></div>

          <div class="wizard-navigation">
            <button type="button" id="step1-next" class="submit-button">Continue</button>
          </div>
        </div>

        <!-- STEP 2: Email -->
        <div id="step-2" class="wizard-step">
          <h2>Step 2: Email Address</h2>

          <div class="form-group">
            <%= f.label :email, "Email Address" %>
            <%= f.email_field :email, id: "migration_email", placeholder: "you@example.com", autocomplete: "email" %>
            <div class="helper-text">We'll send migration status updates to this email</div>
          </div>

          <div class="form-group" style="margin-top: 16px; padding: 16px; background: #fff8e1; border: 2px solid #ffc107; border-radius: 8px;">
            <label style="display: flex; align-items: flex-start; cursor: pointer; user-select: none;">
              <input type="checkbox" id="confirm-email-access" style="margin-right: 12px; margin-top: 3px; width: 18px; height: 18px; cursor: pointer; flex-shrink: 0;">
              <span style="font-size: 15px; line-height: 1.5; color: #333;">
                I confirm that I have access to this email address. I understand that a <strong>PLC token</strong> will be sent to this email during migration, and I must be able to retrieve it to complete the process.
              </span>
            </label>
          </div>

          <div class="wizard-navigation">
            <button type="button" id="step2-back" class="wizard-back-btn">Back</button>
            <button type="button" id="step2-next" class="submit-button">Continue</button>
          </div>
        </div>

        <!-- STEP 3: Destination PDS -->
        <div id="step-3" class="wizard-step">
          <h2>Step 3: Destination PDS</h2>

          <div class="form-group">
            <label for="pds-select">Destination PDS</label>
            <select id="pds-select" class="form-select">
              <option value="">Select a PDS...</option>
              <option value="https://eurosky.social">Eurosky (eurosky.social)</option>
              <option value="https://blacksky.app">Blacksky (blacksky.app)</option>
              <option value="https://myatproto.social">myatproto (myatproto.social)</option>
              <option value="https://bsky.social">Bluesky (bsky.social)</option>
              <option value="custom">Custom PDS...</option>
            </select>
            <div class="helper-text">Choose where you want to migrate your account</div>
          </div>

          <div id="custom-pds-group" class="form-group" style="display: none; margin-top: 16px;">
            <%= f.label :new_pds_host, "Custom PDS URL" %>
            <%= f.text_field :new_pds_host, id: "migration_new_pds_host", placeholder: "https://pds.example.com", autocomplete: "url" %>
            <div class="helper-text">Enter the full URL of your custom PDS</div>
          </div>

          <div id="pds-status" class="lookup-status"></div>

          <div id="invite-code-group" class="form-group" style="display: none; margin-top: 24px;">
            <%= f.label :invite_code, "Invite Code" %>
            <%= f.text_field :invite_code, id: "migration_invite_code", placeholder: "Enter your invite code", autocomplete: "off" %>
            <div class="helper-text">This PDS requires an invite code</div>
          </div>

          <!-- Target PDS password (for migration_in / returning to bsky.social) -->
          <div id="target-password-group" style="display: none; margin-top: 24px; padding: 20px; background: #fff8e1; border: 2px solid #ffc107; border-radius: 8px;">
            <h3 style="margin: 0 0 8px 0; font-size: 16px; color: #f57f17;">Returning to Bluesky</h3>
            <p style="margin: 0 0 16px 0; font-size: 14px; color: #555; line-height: 1.5;">
              Since you're migrating back to bsky.social, we need to verify your old Bluesky password.
              This is the password you used before you migrated away.
            </p>
            <div class="form-group" style="margin: 0;">
              <label for="target-pds-password">Your bsky.social Password</label>
              <input type="password" id="target-pds-password" class="form-control" placeholder="Enter your old bsky.social password" autocomplete="current-password">
              <div class="helper-text">The password for your original Bluesky account</div>
            </div>
            <div id="target-password-status" class="lookup-status"></div>
          </div>

          <!-- Hidden fields for target PDS tokens (populated by JS after verification) -->
          <%= hidden_field_tag 'migration[new_access_token]', '', id: 'migration_new_access_token' %>
          <%= hidden_field_tag 'migration[new_refresh_token]', '', id: 'migration_new_refresh_token' %>

          <div class="wizard-navigation">
            <button type="button" id="step3-back" class="wizard-back-btn">Back</button>
            <button type="button" id="step3-next" class="submit-button">Continue</button>
          </div>
        </div>

        <!-- STEP 4: New Handle -->
        <div id="step-4" class="wizard-step">
          <h2>Step 4: New Handle</h2>

          <!-- Custom Domain Handle Preservation Option -->
          <div id="handle-preservation-section" style="display: none; margin-bottom: 24px; padding: 16px; background: #f0f9ff; border: 2px solid #0ea5e9; border-radius: 8px;">
            <h3 style="margin: 0 0 12px 0; font-size: 18px; color: #0369a1;">🎉 Custom Domain Detected!</h3>
            <p style="margin: 0 0 16px 0; color: #334155;">
              Your handle <strong id="custom-handle-display"></strong> is a custom domain that you control.
              You can keep using this handle on the new PDS!
            </p>

            <div class="form-group" style="margin: 0;">
              <label style="display: flex; align-items: center; cursor: pointer; user-select: none;">
                <input type="checkbox" id="preserve-handle-checkbox" checked style="margin-right: 8px; width: 18px; height: 18px; cursor: pointer;">
                <span style="font-weight: 500;">Keep my current handle</span>
              </label>
              <div class="helper-text" style="margin-left: 26px;">
                Your DNS record will continue to point to your account on the new PDS
              </div>
            </div>
          </div>

          <!-- Regular Handle Input (shown for PDS-hosted handles or when user unchecks preservation) -->
          <div id="new-handle-section" class="form-group">
            <label for="new-handle-input">Your New Handle</label>
            <div style="position: relative;">
              <input type="text" id="new-handle-input" class="form-control" placeholder="username" style="padding-right: 200px;">
              <span id="new-handle-suffix" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); color: #999; pointer-events: none;"></span>
            </div>
            <div class="helper-text">Choose your new handle on the destination PDS</div>
            <%= f.hidden_field :new_handle, id: "migration_new_handle" %>
          </div>

          <div id="step4-status" class="lookup-status"></div>

          <div class="wizard-navigation">
            <button type="button" id="step4-back" class="wizard-back-btn">Back</button>
            <button type="button" id="step4-next" class="submit-button">Continue</button>
          </div>
        </div>

        <!-- STEP 5: Summary & Backup -->
        <div id="step-5" class="wizard-step">
          <h2>Step 5: Review & Confirm</h2>

          <div class="form-group">
            <h3 style="margin-bottom: 16px;">Migration Summary</h3>
            <table style="width: 100%; border-collapse: collapse;">
              <tr style="border-bottom: 1px solid #ddd;">
                <td style="padding: 12px 0; font-weight: 500;">Current Handle:</td>
                <td style="padding: 12px 0;" id="summary-handle"></td>
              </tr>
              <tr style="border-bottom: 1px solid #ddd;">
                <td style="padding: 12px 0; font-weight: 500;">Email:</td>
                <td style="padding: 12px 0;" id="summary-email"></td>
              </tr>
              <tr style="border-bottom: 1px solid #ddd;">
                <td style="padding: 12px 0; font-weight: 500;">Destination PDS:</td>
                <td style="padding: 12px 0;" id="summary-pds"></td>
              </tr>
              <tr style="border-bottom: 1px solid #ddd;">
                <td style="padding: 12px 0; font-weight: 500;">New Handle:</td>
                <td style="padding: 12px 0; font-weight: 600; color: #007bff;" id="summary-new-handle"></td>
              </tr>
              <tr id="summary-invite-row" style="border-bottom: 1px solid #ddd; display: none;">
                <td style="padding: 12px 0; font-weight: 500;">Invite Code:</td>
                <td style="padding: 12px 0;" id="summary-invite"></td>
              </tr>
            </table>
          </div>

           <!-- Backup Bundle Option -->
          <div style="margin-top: 32px; padding: 24px; background: #f0f7ff; border: 2px solid #007bff; border-radius: 8px;">
            <h3 style="margin-top: 0; color: #007bff; font-size: 20px;">📦 Backup Your Data</h3>
            <div style="padding: 16px; background: white; border-radius: 6px; border-left: 4px solid #28a745; margin-bottom: 16px;">
              <strong style="color: #28a745;">✓ Highly Recommended</strong>
              <p style="margin: 8px 0 0 0; font-size: 15px; line-height: 1.6;">
                Download a complete backup of your account data (all posts + media) before migration.
                The backup will be available for 24 hours and sent to your email.
              </p>
              <ul style="margin: 12px 0 0 20px; font-size: 14px; line-height: 1.8;">
                <li>Protects against data loss during migration</li>
                <li>Includes all your posts, media, and account data</li>
                <li>Can be used to restore your account if needed</li>
              </ul>
            </div>
            <div class="form-group" style="margin: 0;">
              <label style="display: flex; align-items: center; cursor: pointer; user-select: none; font-size: 16px; font-weight: 600;">
                <%= f.check_box :create_backup_bundle, id: "migration_create_backup_bundle", checked: true, style: "width: 20px; height: 20px; margin-right: 12px; cursor: pointer;" %>
                <span style="color: #007bff;">Create backup bundle before migration</span>
              </label>
            </div>
          </div>

          <!-- Critical Warning: Do Not Use Account During Migration -->
          <div style="margin-top: 32px; padding: 24px; background: #fef2f2; border: 3px solid #dc2626; border-radius: 8px;">
            <h3 style="margin-top: 0; color: #dc2626; font-size: 20px; display: flex; align-items: center; gap: 8px;">
              <span style="font-size: 24px;">⚠️</span>
              CRITICAL: Do Not Use Your Account During Migration
            </h3>
            <div style="background: white; padding: 20px; border-radius: 6px; border-left: 4px solid #dc2626;">
              <p style="margin: 0 0 16px 0; font-size: 16px; font-weight: 600; color: #991b1b;">
                Once you start the migration, you MUST NOT use your account until it completes.
              </p>
              <p style="margin: 0 0 12px 0; font-size: 15px; line-height: 1.6; color: #333;">
                <strong>Any activity during migration will NOT be transferred:</strong>
              </p>
              <ul style="margin: 0 0 16px 20px; font-size: 15px; line-height: 1.8; color: #333;">
                <li>Posts, replies, or likes made during migration will be lost</li>
                <li>Follows or blocks added during migration will not carry over</li>
                <li>Profile changes will not be included</li>
                <li>Media uploads will not be transferred</li>
              </ul>
              <p style="margin: 0; font-size: 15px; line-height: 1.6; color: #333;">
                <strong>What to do:</strong> Close all apps and browser tabs with your account. Wait for the migration completion email (~20-30 minutes) before logging in again.
              </p>
            </div>
            <div class="form-group" style="margin: 20px 0 0 0;">
              <label style="display: flex; align-items: center; cursor: pointer; user-select: none; font-size: 16px; font-weight: 600;">
                <input type="checkbox" id="acknowledge-no-usage" style="margin-right: 12px; width: 20px; height: 20px; cursor: pointer;" required>
                <span style="color: #991b1b;">I understand I must not use my account during migration</span>
              </label>
            </div>
          </div>

         

          <div class="wizard-navigation">
            <button type="button" id="step5-back" class="wizard-back-btn">Back</button>
            <%= f.submit "Start Migration", class: "submit-button", id: "start-migration-btn" %>
          </div>
        </div>

      <% end %>

      <!-- Image Attribution -->
      <div style="margin-top: 16px; padding: 8px; font-size: 11px; color: #666; text-align: center;">
        Background photo: <a href="https://commons.wikimedia.org/wiki/File:GMC_U-Haul_truck_front_1.JPG" target="_blank" rel="noopener" style="color: #666;">GMC U-Haul truck</a> by <a href="https://commons.wikimedia.org/wiki/User:BrokenSphere" target="_blank" rel="noopener" style="color: #666;">BrokenSphere</a>, <a href="https://creativecommons.org/licenses/by/3.0" target="_blank" rel="noopener" style="color: #666;">CC BY 3.0</a>
      </div>
    </div>
  </div>
</body>
</html>
